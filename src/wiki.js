/* Lua wiki — independent of the builder and generated code */
const WIKI_GROUPS = [{"id": "wiki-0", "title": "기본", "sub": "함수·값·문법", "entries": [{"title": "값과 변수", "sig": "local name = value", "desc": "숫자, 문자열, true/false, nil을 변수에 저장합니다. local은 현재 범위에서만 쓰는 변수입니다.", "example": "local count = 3\nlocal label = \"비\"\nlocal active = true\nlocal missing = nil"}, {"title": "조건과 분기", "sig": "if 조건 then ... else ... end", "desc": "조건에 따라 실행할 동작을 선택합니다. Lua에서 false와 nil만 거짓입니다. 0과 빈 문자열은 참입니다.", "example": "if count >= 10 then\n    log(\"완료\")\nelse\n    log(\"진행 중\")\nend"}, {"title": "함수", "sig": "local function 이름(인자) ... end", "desc": "반복해서 쓸 계산이나 동작에 이름을 붙입니다. return은 계산 결과를 돌려줍니다.", "example": "local function clamp(x, low, high)\n    return math.max(low, math.min(high, x))\nend\nlocal percent = clamp(120, 0, 100)"}, {"title": "테이블", "sig": "{ ... } / table.insert / ipairs", "desc": "Lua 테이블은 목록과 객체 역할을 모두 합니다. 목록은 1번부터 접근합니다.", "example": "local items = {\"맑음\", \"흐림\"}\ntable.insert(items, \"비\")\nlocal profile = { name = \"Arisu\", day = 10 }\nlog(items[1])\nlog(profile.day)"}, {"title": "문자열과 숫자", "sig": "tostring / tonumber / ..", "desc": ".. 는 문자열 연결입니다. 숫자가 들어올 수도 있는 State 값은 tonumber으로 변환할 수 있습니다.", "example": "local n = tonumber(\"12\") or 0\nlocal line = \"Day \" .. tostring(n)"}]}, {"id": "wiki-1", "title": "이벤트", "sub": "언제 실행되나요?", "entries": [{"title": "사용자 입력", "sig": "onInput(triggerId)", "desc": "사용자 입력 이벤트에서 호출되는 콜백입니다. getFullChat으로 현재 채팅을 읽을 수 있습니다.", "example": "function onInput(triggerId)\n    log(\"입력 이벤트\")\nend"}, {"title": "AI 출력", "sig": "onOutput(triggerId)", "desc": "AI 응답을 받은 뒤 호출되는 콜백입니다.", "example": "function onOutput(triggerId)\n    log(\"출력 이벤트\")\nend"}, {"title": "시작 이벤트", "sig": "onStart(triggerId)", "desc": "메시지 전송 처리 과정의 시작 이벤트 콜백입니다. 첫 대화 시작만 의미하는 것은 아닙니다.", "example": "function onStart(triggerId)\n    log(\"시작 이벤트\")\nend"}, {"title": "텍스트 편집", "sig": "listenEdit(type, callback)", "desc": "editInput / editOutput / editDisplay / editRequest 이벤트에서 callback(triggerId, data)를 받습니다. 변경한 값을 return합니다. data 구조는 이벤트에 따라 다를 수 있습니다.", "example": "listenEdit(\"editInput\", function(triggerId, data)\n    return data .. \"\\n[추가 문구]\"\nend)"}, {"title": "버튼 이벤트", "sig": "function 버튼이름(triggerId)", "desc": "Risu의 버튼 마크업에서 지정한 이름과 같은 전역 함수를 호출합니다.", "example": "function showInfo(triggerId)\n    alertNormal(triggerId, \"정보\")\nend\n-- {{button::정보::showInfo}}"}]}, {"id": "wiki-2", "title": "State", "sub": "상태 읽기·저장", "entries": [{"title": "State 읽기", "sig": "getState(triggerId, key)", "desc": "key에 해당하는 저장 상태를 반환합니다. 처음 실행했을 때 값이 없다면 기본값을 지정하세요.", "example": "local state = getState(triggerId, \"tracker\") or {}\nlocal count = tonumber(state.count) or 0"}, {"title": "State 저장", "sig": "setState(triggerId, key, value)", "desc": "같은 key에 값을 저장합니다. 테이블을 수정했다면 저장 함수를 호출해 변경을 반영하세요.", "example": "local state = getState(triggerId, \"tracker\") or {}\nstate.count = (tonumber(state.count) or 0) + 1\nsetState(triggerId, \"tracker\", state)"}, {"title": "채팅 변수", "sig": "getChatVar / setChatVar", "desc": "Risu의 채팅 변수를 읽고 설정합니다. State 테이블과 구별해서 사용하세요.", "example": "local name = getChatVar(triggerId, \"place\")\nsetChatVar(triggerId, \"place\", \"Rome\")"}, {"title": "JSON", "sig": "json.encode / json.decode", "desc": "테이블을 JSON 문자열로 바꾸거나 JSON 문자열을 테이블로 읽습니다. nil은 JSON의 null과 같지 않을 수 있습니다.", "example": "local encoded = json.encode({ count = 3 })\nlocal decoded = json.decode(encoded)"}]}, {"id": "wiki-3", "title": "채팅", "sub": "메시지 읽기·변경", "entries": [{"title": "전체 채팅", "sig": "getFullChat(triggerId)", "desc": "현재 채팅 메시지 목록을 읽습니다. 각 메시지의 role과 data를 확인할 수 있습니다.", "example": "local chat = getFullChat(triggerId)\nlocal last = chat[#chat]\nif last then log(last.data) end"}, {"title": "채팅 길이", "sig": "getChatLength(triggerId)", "desc": "메시지 개수를 반환합니다. Lua 배열은 1부터 시작하지만 Risu 채팅 API 인덱스는 0부터 시작합니다.", "example": "local length = getChatLength(triggerId)\nlocal lastIndex = length - 1"}, {"title": "메시지 추가", "sig": "addChat / insertChat", "desc": "role은 user 또는 char를 사용합니다. insertChat의 index는 0부터 시작합니다.", "example": "addChat(triggerId, \"user\", \"추가 메시지\")\ninsertChat(triggerId, 0, \"char\", \"맨 앞 메시지\")"}, {"title": "메시지 수정", "sig": "setChat / setChatRole", "desc": "0-based index에 있는 메시지 본문이나 role을 바꿉니다.", "example": "setChat(triggerId, 0, \"수정된 메시지\")\nsetChatRole(triggerId, 0, \"user\")"}, {"title": "메시지 제거", "sig": "removeChat / cutChat", "desc": "removeChat은 0-based index의 메시지 하나를 제거합니다. cutChat은 범위를 다루므로 실제 채팅에 적용하기 전 확인하세요.", "example": "local n = getChatLength(triggerId)\nif n > 0 then removeChat(triggerId, n - 1) end"}, {"title": "생성 중단", "sig": "stopChat(triggerId)", "desc": "현재 생성 흐름을 중단하도록 요청합니다. 이벤트 시점에 따라 동작이 달라질 수 있습니다.", "example": "function onStart(triggerId)\n    stopChat(triggerId)\nend"}]}, {"id": "wiki-4", "title": "알림·디버그", "sub": "실행 결과 확인", "entries": [{"title": "디버그 로그", "sig": "log(value)", "desc": "중간 값이나 실행 경로를 기록합니다. 어떤 조건이 통과했는지 확인할 때 사용합니다.", "example": "log({ day = 10, active = true })"}, {"title": "일반 알림", "sig": "alertNormal(triggerId, message)", "desc": "사용자에게 일반 알림을 표시합니다.", "example": "alertNormal(triggerId, \"저장했습니다.\")"}, {"title": "오류 알림", "sig": "alertError(triggerId, message)", "desc": "오류 상태를 사용자에게 알립니다.", "example": "alertError(triggerId, \"날짜 형식을 확인하세요.\")"}, {"title": "입력 요청", "sig": "alertInput(triggerId, message)", "desc": "사용자에게 문자열 입력을 요청합니다. 호출 위치와 취소 시 반환값을 고려하세요.", "example": "local name = alertInput(triggerId, \"대상 이름\")\nif name and name ~= \"\" then log(name) end"}]}, {"id": "wiki-5", "title": "계산", "sub": "숫자·랜덤·문자열", "entries": [{"title": "산술과 나머지", "sig": "+ - * / % ^", "desc": "사칙연산과 나머지, 거듭제곱입니다. 주기 계산에서는 %를 사용할 수 있습니다. 0으로 나누는 경우를 처리하세요.", "example": "local cycleDay = ((elapsed % 27) + 27) % 27 + 1\nlocal percent = total ~= 0 and current / total * 100 or 0"}, {"title": "범위 제한", "sig": "math.min / math.max", "desc": "최솟값·최댓값을 이용해 계산 결과가 지정 범위를 넘지 않게 합니다.", "example": "local progress = math.max(0, math.min(100, rawProgress))"}, {"title": "반올림", "sig": "math.floor / math.ceil", "desc": "floor는 내림, ceil은 올림입니다. 양수에 한해서 floor(x + 0.5)로 일반적인 반올림을 구현할 수 있습니다.", "example": "local down = math.floor(2.9)\nlocal up = math.ceil(2.1)"}, {"title": "랜덤", "sig": "math.random(min, max)", "desc": "정수 범위에서 무작위 값을 뽑습니다. 동일한 날의 결과를 유지하려면 한 번 뽑은 값을 State에 저장하세요.", "example": "local roll = math.random(1, 100)\nlocal hit = roll <= 25"}, {"title": "문자열 검사", "sig": "string.find / string.match", "desc": "find(..., 1, true)는 일반 문자열을 그대로 찾습니다. match는 Lua pattern 규칙을 사용하며 JS 정규식과 문법이 다릅니다.", "example": "local isCommand = string.sub(input, 1, 3) == \"/상\"\nlocal found = string.find(input, \"storm\", 1, true) ~= nil"}, {"title": "문자열 치환", "sig": "string.gsub", "desc": "Lua pattern에 일치하는 부분을 치환합니다. 바꿀 값에 %가 들어가면 Lua 치환 규칙에 주의하세요.", "example": "local cleaned = (input:gsub(\"%s+\", \" \"))"}, {"title": "날짜 계산", "sig": "연·월·일 / 일수 차이", "desc": "Lua 날짜 계산은 세계관의 달력 규칙에 따라 달라집니다. 이 빌더의 날짜 재료는 YYYY-MM-DD 형식의 그레고리력 계산을 사용합니다.", "example": "local year, month, day = (\"2026-09-19\"):match(\"^(%d%d%d%d)%-(%d%d)%-(%d%d)$\")"}, {"title": "HTML 출력", "sig": "editDisplay + return", "desc": "표시용 문자열에 HTML을 조합할 수 있습니다. Risu 렌더링과 sanitizer에 따라 스타일·태그 지원은 달라집니다.", "example": "listenEdit(\"editDisplay\", function(triggerId, data)\n    local state = getState(triggerId, \"hud\") or {}\n    return data .. \"<div>\" .. tostring(state.score or 0) .. \"</div>\"\nend)"}]}, {"id": "wiki-6", "title": "주의", "sub": "혼동하기 쉬운 부분", "entries": [{"title": "Lua와 JS 플러그인", "sig": "Risu Lua ≠ plugin JavaScript", "desc": "Lua 트리거에는 Risu Lua 전용 함수를 사용합니다. JS 플러그인의 Risuai 객체와 await 문법을 Lua에 그대로 넣지 않습니다.", "example": "-- Lua 트리거\nlocal state = getState(triggerId, \"key\")"}, {"title": "인덱스 기준", "sig": "Lua 배열 1 / Risu 채팅 API 0", "desc": "chat[#chat]은 Lua 배열의 마지막 메시지입니다. removeChat에는 getChatLength(triggerId)-1을 전달합니다.", "example": "local chat = getFullChat(triggerId)\nlocal last = chat[#chat]\nremoveChat(triggerId, getChatLength(triggerId) - 1)"}, {"title": "패턴 문법", "sig": "Lua pattern ≠ JavaScript RegExp", "desc": "Lua에서는 %d, %s, [abc], ^, $ 등을 사용합니다. JS의 \\d, (?:...), lookbehind 같은 문법을 그대로 복사하지 마세요.", "example": "local number = (\"day 12\"):match(\"%d+\")"}, {"title": "편집 이벤트 반환", "sig": "return data", "desc": "edit 계열에서 변경한 문자열은 콜백의 return으로 전달해야 합니다. 일반 onInput 등과 역할이 다릅니다.", "example": "listenEdit(\"editOutput\", function(triggerId, data)\n    return data:gsub(\"old\", \"new\")\nend)"}]}];
WIKI_GROUPS.unshift({"id": "wiki-concepts", "title": "선행 개념", "sub": "처음 보는 용어부터", "entries": [{"title": "Lua와 RisuAI Lua", "sig": "코드 / 스크립트", "desc": "Lua는 컴퓨터에게 할 일을 적는 언어입니다. RisuAI Lua는 그 언어로 채팅·상태·출력을 다루는 스크립트예요. 빌더에서 재료를 고르면 Lua 코드가 생성되고, RisuAI에서 실행합니다.", "example": "-- 이 줄은 설명(주석)입니다.\nlog(\"안녕하세요\")"}, {"title": "값과 자료형", "sig": "숫자 · 글자 · 참/거짓 · 빈값", "desc": "값은 프로그램이 다루는 재료입니다. 10은 숫자, \"10\"은 글자(문자열), true/false는 참·거짓, nil은 값이 없음을 뜻합니다. 숫자 10과 글자 \"10\"은 같은 재료가 아니에요.", "example": "local days = 10       -- 숫자\nlocal label = \"10\"    -- 문자열\nlocal active = true   -- 참/거짓\nlocal missing = nil   -- 값 없음"}, {"title": "변수란?", "sig": "이름 붙인 임시 보관함", "desc": "변수는 계산 도중 값을 넣어 두고 이름으로 다시 사용하는 보관함입니다. 아래 days라는 이름에 숫자 3을 담았다가 1을 더합니다. 변수에 들어 있는 값은 실행 중 바뀔 수 있어요.", "example": "local days = 3\ndays = days + 1\nlog(days) -- 4"}, {"title": "State란?", "sig": "다음 실행에도 읽어 올 상태", "desc": "State는 플러그인이 기억해야 할 현재 정보예요. 예를 들어 회복 추적 중인지, 시작일과 진행률이 얼마인지 담습니다. Lua의 일반 변수는 실행 중 잠깐 쓰는 반면, getState/setState는 RisuAI에 저장된 값을 다음 실행에 다시 읽고 갱신하는 데 사용합니다.", "example": "local state = getState(triggerId, \"recovery\") or {}\nstate.active = true\nstate.startDay = \"2026-09-19\"\nsetState(triggerId, \"recovery\", state)"}, {"title": "State key와 필드", "sig": "서랍 이름 / 서랍 안의 항목", "desc": "key는 어떤 상태 묶음을 읽을지 구분하는 이름입니다(예: \"recovery\"). 필드는 그 안에 저장하는 개별 항목(예: active, startDay)이에요. 같은 key로 읽고 저장해야 기존 상태를 이어서 사용할 수 있습니다.", "example": "local state = getState(triggerId, \"recovery\") or {}\nlocal active = state.active\nstate.active = true\nsetState(triggerId, \"recovery\", state)"}, {"title": "테이블과 목록", "sig": "여러 값을 한데 담는 상자", "desc": "테이블은 여러 값을 묶는 Lua의 자료구조입니다. 이름이 붙은 항목을 모으면 상태 객체처럼, 순서대로 모으면 목록처럼 쓸 수 있어요. 일반 Lua 목록은 첫 번째가 1번입니다.", "example": "local tracker = { active = true, days = 3 }\nlog(tracker.days) -- 3\nlocal skies = { \"맑음\", \"흐림\", \"비\" }\nlog(skies[1]) -- 맑음"}, {"title": "함수란?", "sig": "이름 붙인 작업 또는 계산", "desc": "함수는 여러 동작을 하나로 묶고 이름을 붙인 것입니다. 필요할 때 이름으로 실행할 수 있어요. 예를 들어 addDays는 두 값을 받아 더한 결과를 돌려줍니다. 직접 작성하기보다 빌더에서 계산 재료를 골라도 됩니다.", "example": "local function addDays(current, amount)\n    return current + amount\nend\nlocal tomorrow = addDays(10, 1) -- 11"}, {"title": "인자란?", "sig": "함수에 건네는 입력값", "desc": "함수를 사용할 때 괄호 안에 넣어 주는 값입니다. getState(triggerId, \"recovery\")에서 triggerId는 어떤 실행인지 알려 주는 값, \"recovery\"는 읽을 State의 이름이에요. 함수마다 받는 인자의 개수와 의미가 다릅니다.", "example": "local state = getState(triggerId, \"recovery\")\n-- getState(실행 식별자, 상태 이름)"}, {"title": "return이란?", "sig": "함수가 돌려주는 결과", "desc": "함수의 실행 결과를 호출한 쪽에 전달합니다. editInput 같은 텍스트 편집 콜백은 바꾼 텍스트를 return해야 반영됩니다. return false처럼 특별한 값을 돌려주는 경우는 이벤트 종류와 RisuAI의 처리 규칙에 따라 의미가 달라집니다.", "example": "listenEdit(\"editInput\", function(triggerId, data)\n    return data .. \" [추가]\"\nend)"}, {"title": "트리거와 이벤트", "sig": "언제 시작할지 정하는 신호", "desc": "이벤트는 사용자 입력, AI 출력, 화면 표시처럼 RisuAI에서 일어나는 일입니다. 트리거는 그 시점에 실행할 Lua를 연결하는 방식이에요. onInput은 사용자 입력 때 실행하는 예시입니다.", "example": "function onInput(triggerId)\n    log(\"사용자 입력 이벤트\")\nend"}, {"title": "콜백이란?", "sig": "이벤트가 발생하면 불리는 함수", "desc": "콜백은 이벤트가 일어났을 때 RisuAI가 실행하도록 전달해 둔 함수입니다. listenEdit는 텍스트를 가공할 때 실행할 함수를 등록합니다. data에는 그 이벤트가 건네준 내용이 들어옵니다.", "example": "listenEdit(\"editDisplay\", function(triggerId, data)\n    return data .. \"<div>표시문</div>\"\nend)"}, {"title": "조건과 분기", "sig": "어떤 경우에만 실행할지", "desc": "조건은 참/거짓을 검사하는 규칙입니다. if는 조건이 참일 때의 동작, else는 아닐 때의 동작을 정합니다. AND(and)는 둘 다 참, OR(or)는 하나라도 참인 경우입니다.", "example": "local progress = 80\nif progress >= 100 then\n    log(\"완료\")\nelse\n    log(\"진행 중\")\nend"}, {"title": "문자열과 숫자 계산", "sig": "글자 연결과 수치 계산", "desc": "문자열은 따옴표로 감싼 글자입니다. 숫자는 더하기·나누기 등을 할 수 있어요. 문자열을 합칠 땐 .. 를 쓰고, 숫자로 바꾸려면 tonumber을 사용합니다.", "example": "local days = 3\nlocal title = \"Day \" .. tostring(days)\nlocal n = tonumber(\"12\") -- 12"}, {"title": "nil과 기본값", "sig": "아직 저장된 정보가 없을 때", "desc": "처음에는 State나 필드가 없어서 nil일 수 있어요. or {}는 값이 없을 때 빈 테이블을 대신 쓰라는 뜻입니다. 단, false도 or의 기본값으로 넘어가므로 참/거짓 상태에는 nil인지 따로 확인하는 편이 안전합니다.", "example": "local state = getState(triggerId, \"tracker\") or {}\nlocal count = tonumber(state.count) or 0\nif state.active == nil then\n    state.active = false\nend"}, {"title": "프로젝트·코드·실행", "sig": "조립 → Lua 생성 → Risu에서 사용", "desc": "프로젝트 JSON은 조립 설정을 다시 편집하기 위한 파일입니다. .lua는 실제로 생성된 Lua 코드예요. 간이 시뮬레이션은 계산 흐름 확인용이며 RisuAI에서의 실제 이벤트 동작을 완전히 재현하지는 않습니다.", "example": "-- .lua 파일의 예시\nfunction onInput(triggerId)\n    log(\"실행됨\")\nend"}]});
let wikiCategory='all';
function wikiEl(tag, cls, content){
  const el=document.createElement(tag); if(cls)el.className=cls;
  if(content!==undefined)el.textContent=content; return el;
}
function renderWiki(){
  const nav=document.getElementById('wikiNav');
  const articles=document.getElementById('wikiArticles');
  const term=document.getElementById('wikiSearch').value.trim().toLocaleLowerCase();
  nav.replaceChildren(); articles.replaceChildren();
  const choices=[{id:'all',title:'전체'},...WIKI_GROUPS];
  choices.forEach(group=>{
    const btn=wikiEl('button',wikiCategory===group.id?'active':'',group.title);
    btn.type='button';btn.onclick=()=>{wikiCategory=group.id;renderWiki()}; nav.append(btn);
  });
  let found=0;
  WIKI_GROUPS.filter(g=>wikiCategory==='all'||wikiCategory===g.id).forEach(group=>{
    const matches=group.entries.filter(e=>[group.title,group.sub,e.title,e.sig,e.desc,e.example].join(' ').toLocaleLowerCase().includes(term));
    if(!matches.length)return;
    articles.append(wikiEl('h2','wiki-group-title',group.title));
    articles.append(wikiEl('div','wiki-subtitle',group.sub));
    matches.forEach(e=>{
      const details=wikiEl('details','wiki-entry');
      details.open=Boolean(term);
      const summary=wikiEl('summary');
      summary.append(wikiEl('b','',e.title),wikiEl('code','',e.sig));
      const body=wikiEl('div','wiki-entry-body');
      body.append(wikiEl('p','',e.desc),wikiEl('pre','',e.example));
      details.append(summary,body);articles.append(details);found++;
    });
  });
  if(!found)articles.append(wikiEl('div','wiki-noresults','검색 결과 없음'));
}
document.getElementById('wikiBtn').onclick=()=>{toPage('wiki');renderWiki();window.scrollTo({top:0,behavior:'instant'})};
document.getElementById('wikiSearch').oninput=renderWiki;
document.getElementById('wikiExpand').onclick=()=>{
  const items=[...document.querySelectorAll('#wikiArticles details')];
  const willOpen=items.some(d=>!d.open);
  items.forEach(d=>d.open=willOpen);
  document.getElementById('wikiExpand').textContent=willOpen?'모두 접기':'모두 펼치기';
};


